## 2026-01-12 - Missing Skip Link Styles
**Learning:** Even if a theme can't add HTML elements, it must provide styles for standard accessible elements (like .mw-jump-link) which might otherwise be invisible or broken by the theme's overrides.
**Action:** Always check for standard MediaWiki accessibility classes (.mw-jump-link) and ensure they have visible focus states in the theme.
