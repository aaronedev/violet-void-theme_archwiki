@import '../variables/colors'
@import '../variables/layout'
@import '../mixins/effects'

// Arch navigation bar
#archnavbar
  height 40px !important
  padding 10px 15px !important
  background $base !important
  border-bottom 5px $arch-blue solid !important
  display flex
  align-items center

// Sidebar
.mw-sidebar
  background linear-gradient(180deg, $dark, $darker)
  border-radius $border-radius-md
  box-shadow $shadow-subtle
  padding 1em
  border 1px solid $border-subtle

// Portal headings
.vector-menu-portal .vector-menu-heading
  color $lighter
  font-size 0.85em
  font-weight 600
  text-transform uppercase
  letter-spacing 0.05em
  border-bottom 2px solid $secondary-blue
  padding-bottom 0.5em
  margin-bottom 0.5em
  display block

// Portal content
.vector-menu-portal .vector-menu-content li
  margin 0.5em 0
  font-size 0.95em

// Table of contents
#toc, .toc
  background linear-gradient(135deg, $base, rgba($arch-blue, 0.04))
  border 1px solid $border-subtle
  border-radius $border-radius-md
  padding 1em
  box-shadow $shadow-subtle
  transition all $transition-medium
  
  &:hover
    box-shadow $shadow-elevated
    border-color rgba($arch-blue, 0.2)

.tocnumber
  color $secondary-blue
  font-weight 600
  margin-right 0.5em

// TOC toggle
.toctogglelabel
  color $secondary-blue
  font-size 0.9em
  cursor pointer

  &:hover
    color $orange
