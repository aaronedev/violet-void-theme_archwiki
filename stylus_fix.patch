*** Begin Patch
*** Update File: src/components/navigation.styl
@@
-.vector-toc .vector-toc-toggle
-  // FIX: Position the toggle absolutely to align with text and prevent layout shifts
-  position absolute
-  left 0.25em
-  top 0.35em
-  background-color $darker
-  border 1px solid $border-subtle
-  color $light
-  border-radius $border-radius-sm
-  min-width 20px
-  min-height 20px
-  display flex
-  align-items center
-  justify-content center
-  cursor pointer
-
-  &:hover
-    background-color rgba($arch-blue, 0.1)
-    color $secondary-blue
-
-// Icon within toggle buttons
-.vector-toc .vector-toc-toggle .vector-icon
-  fill $light
+// Position the table‑of‑contents toggle buttons relative to the list item so
+// their icons align vertically with the menu text.  Use a 50% offset and
+// negative translation to achieve perfect centering regardless of line height.
+.vector-toc .vector-toc-toggle
+  position absolute
+  // leave a little more horizontal space so the toggle sits flush with the
+  // numbered bullets; increasing the left offset centres the icon better
+  left 0.5em
+  top 50%
+  transform translateY(-50%)
+  background-color $darker
+  border 1px solid $border-subtle
+  color $light
+  border-radius $border-radius-sm
+  min-width 20px
+  min-height 20px
+  display flex
+  align-items center
+  justify-content center
+  cursor pointer
+
+  &:hover
+    background-color rgba($arch-blue, 0.1)
+    color $secondary-blue
+
+// Icon within toggle buttons
+.vector-toc .vector-toc-toggle .vector-icon
+  fill $light
*** End Patch
